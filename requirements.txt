# 项目依赖包列表
# 基础与数据处理
Flask>=2.3.0
Werkzeug>=2.3.0
Jinja2>=3.1.0
numpy>=1.24.0
pandas>=1.5.0
Pillow>=9.5.0
opencv-python-headless>=4.8.0  # 使用headless版本，避免GUI依赖

# 深度学习框架与相关
torch>=2.0.0
torchvision>=0.15.0
albumentations>=1.3.0  # 用于数据增强
scikit-learn>=1.3.0  # 用于评估指标计算

# 可视化
matplotlib>=3.7.0
seaborn>=0.12.0

# 开发与工具（可选，但推荐）
python-dotenv>=1.0.0  # 管理环境变量
# 更多依赖将在开发中按需添加...